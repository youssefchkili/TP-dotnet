@model IEnumerable<MyFirstApp.Models.AuditLog>

@{
    ViewData["Title"] = "Historique des Audits";
}

<div class="container mt-4">
    <h1>📋 Historique des Audits</h1>
    <p class="text-muted">Suivi de toutes les modifications apportées à la base de données</p>

    <hr />

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Table</th>
                <th>Action</th>
                <th>Clé Entité</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var log in Model)
            {
                <tr>
                    <td>@log.Id</td>
                    <td>
                        <span class="badge bg-info">@log.TableName</span>
                    </td>
                    <td>
                        @if (log.Action == "Added")
                        {
                            <span class="badge bg-success">✚ Ajouté</span>
                        }
                        else if (log.Action == "Modified")
                        {
                            <span class="badge bg-warning text-dark">✎ Modifié</span>
                        }
                        else if (log.Action == "Deleted")
                        {
                            <span class="badge bg-danger">✖ Supprimé</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">@log.Action</span>
                        }
                    </td>
                    <td><code>@log.EntityKey</code></td>
                    <td>@log.Date.ToLocalTime().ToString("dd/MM/yyyy HH:mm:ss")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@log.Id" class="btn btn-sm btn-outline-primary">
                            Détails
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            <strong>ℹ️ Aucun audit enregistré</strong>
            <p>Les modifications futures seront automatiquement enregistrées ici.</p>
        </div>
    }

    <div class="mt-3">
        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Retour à l'accueil</a>
    </div>
</div>
